import { RegisterForm } from "components/RegisterForm/RegisterForm";
import { Wrapper } from "pages/Register/Register.styled";

const Register = () => {
  return (
    <Wrapper>
      <RegisterForm />
    </Wrapper>
  );
};

export default Register;